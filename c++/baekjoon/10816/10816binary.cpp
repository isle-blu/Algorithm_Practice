#include <stdio.h>
#include <vector>
#include <algorithm>
using namespace std;

vector<pair<int, int>> card;

int main()
{
    int N, M;
    scanf("%d", &N);

    int tmp;
    for (int i = 0; i < N; i++)
    {
    }

    scanf("%d", &M);

    for (int i = 0; i < M; i++)
    {
    }

    return 0;
}